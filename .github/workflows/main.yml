on: [push]

jobs:
  merge_conflict_job:
    runs-on: ubuntu-latest
    name: Chexk for merge conflicts
    steps:
      - name: Check for Conflicts Action
        uses: actions/check-for-conflicts-action@v1
        with:
          who-to-greet: 'Noviicee'
